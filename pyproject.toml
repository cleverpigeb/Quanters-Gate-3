[project]
name = "Quanters-Gate-3"
version = "0.1.0"
description = "Three adventurers, one goal: find the Alpha, slay the drawdown, and claim the Gate"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "akshare>=1.18.64",
    "matplotlib>=3.11.0",
    "numpy>=2.0.0",
    "pandas>=3.0.3",
]
